import 'package:flutter/material.dart';

// Define the colors used in the app
class AppColors {
  static const Color primary = Color.fromARGB(255, 198, 33, 243);
  static const Color secondary = Colors.green;
  static const Color background = Color.fromARGB(255, 27, 27, 27);
  static const Color textPrimary = Color.fromARGB(255, 215, 215, 215);
  static const Color textSecondary = Colors.grey;
  static const Color error = Colors.red;
  static const Color textError = Colors.red;
  static const Color surface = Color.fromARGB(255, 37, 37, 37);
}
